# @vercel/analytics declares an *optional* peer on @sveltejs/kit, which npm's
# strict peer resolver tries to satisfy and clashes with Vitest's vite@5.
# We don't use Svelte; relaxing peer resolution avoids the false conflict
# (and keeps Vercel's `npm install` from failing the same way).
legacy-peer-deps=true
